<resources>

    <!-- Base application theme. -->
    <style name="BlackTheme" parent="@android:style/Theme.Holo">
        <!--<item name="android:buttonStyle">@style/BlackButtonStyle</item>-->
    </style>
    <style name="WhiteTheme" parent="@android:style/Theme.Holo.Light">
        <!--<item name="android:buttonStyle">@style/WhiteButtonStyle</item>-->
    </style>
    <style name="TextViewStyle">
        <item name="android:maxLength">3</item>
        <item name="android:textSize">16sp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">#2e2e2e</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textColor">#ffffff</item>
    </style>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="WhiteButtonStyle" parent="android:Widget.Button">
        <item name="android:background">@drawable/white_button_background</item>
        <item name="android:gravity">center</item>
    </style>
    <style name="BlackButtonStyle" parent="android:Widget.Button">
        <item name="android:background">@drawable/black_button_background</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
